const assert = require('chai').assert,
      expect = require('chai').expect,
      should = require('chai').should;

describe('test InputStream', () => {
    let InputStream = require('../InputStream.js');
     
     
});

describe('test Tokenenize', () => {


});
